# -- CORS SETTING -- Uncomment the below line for local development, comment out for production
VITE_API_URL=http://localhost:5001/api 
# -- CORS SETTING -- Uncomment the below line for a production server (use your registered domain name)
# VITE_API_URL=https://your-domain.com/api
# Development environment - no beta disclaimer by default
VITE_SHOW_BETA_DISCLAIMER=false
VITE_BETA_DISCLAIMER_TEXT="This beta tool serves as an aid for validation but should not be used independently. <strong> Solutions Architect validation criteria remains mandatory.</strong>"