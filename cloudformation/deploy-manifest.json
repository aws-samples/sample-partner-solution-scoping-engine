{
  "deployment_order": [
    {
      "name": "${StackPrefix}-01-dns",
      "template": "01-route53.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED",
        "PublicReachableDomainName": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-02-security",
      "template": "02-security.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED",
        "PublicReachableDomainName": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-03-vpc",
      "template": "03-vpc.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-04-s3",
      "template": "04-s3.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-05-cloudfront",
      "template": "05-cloudfront.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED",
        "CloudFrontPrivateKey": "REQUIRED",
        "CloudFrontPublicKey": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-06-bedrock",
      "template": "06-bedrock.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-07-dynamodb",
      "template": "07-dynamodb.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-08-elasticache",
      "template": "08-elasticache.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-09-cognito-waf",
      "template": "09-cognito-waf.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-10-alb-cognito",
      "template": "10-alb-cognito.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED",
        "PublicReachableDomainName": "REQUIRED",
        "EnableExternalIdP": "false",
        "IdPProviderName": "",
        "IdPProviderType": "OIDC",
        "IdPClientId": "",
        "IdPClientSecret": "",
        "IdPOIDCIssuer": "",
        "IdPAuthorizeScopes": "openid email profile groups"
      }
    },
    {
      "name": "${StackPrefix}-11-load-balancer-waf",
      "template": "11-load-balancer-waf.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-12-ec2role",
      "template": "12-ec2role.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-13-compute",
      "template": "13-compute.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED",
        "PublicReachableDomainName": "REQUIRED",
        "SeraVersion": "REQUIRED",
        "PartnerName": "REQUIRED",
        "PartnerLogoUrl": "REQUIRED",
        "PartnerEmail": "REQUIRED",
        "EnableExternalIdP": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-14-cloudtrail",
      "template": "14-cloudtrail.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED"
      }
    },
    {
      "name": "${StackPrefix}-15-cloudtrail-monitoring",
      "template": "15-cloudtrail-monitoring.yaml",
      "parameters": {
        "StackPrefix": "REQUIRED",
        "AlertEmail": "OPTIONAL"
      }
    }
  ]
}
